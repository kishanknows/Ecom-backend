services:
  api:
    image:
      kishanknows/ecom-api:latest

    ports:
      - "8080:8080"

    env_file:
      - .env

    networks:
      - monitoring

networks:
  monitoring:
    external: true
